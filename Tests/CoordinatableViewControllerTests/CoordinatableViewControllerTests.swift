import XCTest
@testable import CoordinatableViewController

final class CoordinatableViewControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CoordinatableViewController().text, "Hello, World!")
    }
}
